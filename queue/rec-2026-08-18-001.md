---
id: rec-2026-08-18-001
record_type: test-case
candidate_ref: examples/candidates/test-2026-08-18.md
provenance_ref: trace://review-queue/ci/2026-08-18.jsonl#evt-7
diff: "--- /dev/null\n+++ tests/test_loader_bare_scheme.py\n@@ -0,0 +1,3 @@\n+def test_loader_drops_bare_scheme_provenance():\n+    rec = make_record(provenance_ref=\"trace:run/1\")  # missing //\n+    assert not score._row_is_eligible(rec)\n"
status: pending
target: "file://tests/test_loader_bare_scheme.py"
created_at: 2026-08-18T09:12:00Z
verdicts: []
---

# Candidate diff

```diff
--- /dev/null
+++ tests/test_loader_bare_scheme.py
@@ -0,0 +1,3 @@
+def test_loader_drops_bare_scheme_provenance():
+    rec = make_record(provenance_ref="trace:run/1")  # missing //
+    assert not score._row_is_eligible(rec)
```

# Provenance

Produced by the `eval-distiller` agent while triaging a CI run where the
scoring loader counted a malformed provenance ref. The trace ref points
at the event-log row recording the producing run.

# Why it is queued

This is a pending candidate: a real test case waiting on a human
verdict. It exercises the queue without a recorded decision, so `list
--status pending` and the streamlit "pending" view have a live row.
