# Skill: redact-check before submit

A pre-submit skill that scans a candidate payload for raw/private path
prefixes (`/Users/`, `C:\\Users\\`, `~/.ssh`) and refuses to enqueue
the record if any are present, so private paths never land in a
committed promotion-record diff.
