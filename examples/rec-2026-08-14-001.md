---
id: rec-2026-08-14-001
record_type: memory-update
candidate_ref: examples/candidates/mem-2026-08-14.md
provenance_ref: trace://athena-site/ops/event-log/2026-08-14.jsonl#evt-42
diff: "--- /dev/null\n+++ examples/candidates/mem-2026-08-14.md\n@@ -0,0 +1,7 @@\n+# Memory: scoring-pipeline-quirks\n+\n+The athena-site scoring pipeline drops calibration samples whose\n+provenance ref does not start with `trace://` or `evidence://`. When\n+debugging an unexpectedly small calibration set, check\n+`scoring/loader.py` for the schema filter applied at row ingestion.\n"
status: published
target: "file://docs/memory/scoring-pipeline-quirks.md"
created_at: 2026-08-14T12:34:56Z
verdicts:
  - reviewer: vignesh
    decided_at: 2026-08-14T13:01:12Z
    decision: approve
    note: "Trace confirms the loader filter behavior. Memory worth keeping."
---

# Candidate diff

```diff
--- /dev/null
+++ examples/candidates/mem-2026-08-14.md
@@ -0,0 +1,7 @@
+# Memory: scoring-pipeline-quirks
+
+The athena-site scoring pipeline drops calibration samples whose
+provenance ref does not start with `trace://` or `evidence://`. When
+debugging an unexpectedly small calibration set, check
+`scoring/loader.py` for the schema filter applied at row ingestion.
```

# Provenance

The candidate was produced by the `memory-distiller` agent on
2026-08-14 while reviewing an athena-site oncall thread. The trace ref
above points at the event-log row recording the producing run, including
the model id and the source thread message.

# Verdict context

The reviewer ran the loader code path against the recorded trace and
confirmed the filter behavior described in the candidate. This is the
first calibration row for ReviewQueue: a real candidate, a real
provenance ref, a real human verdict, a real target file. The record
is checked in at `published` status because the target file was
written and the ledger is closed for this row.

# Calibration

This row is the seed observation for the v0 calibration set. The
question it answers: "Does the human-gate flow preserve enough context
for a reviewer to ratify or refuse a candidate within one minute?" The
recorded verdict latency (~26 minutes from queue to verdict) is the
first calibration data point. Future rows should record the same
latency field once the schema lands.
